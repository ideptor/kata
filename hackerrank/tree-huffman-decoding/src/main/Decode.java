package main;

public class Decode {

	public static String decode(String encoded, Node root) {
		StringBuffer buf = new StringBuffer();
		
		for(int i=0; i<encoded.length(); i++) {
			Node next = (encoded.charAt(i)=='0')? 
		}
		
		
		return buf.toString();
	}

}
